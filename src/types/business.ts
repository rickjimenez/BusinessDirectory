export interface TBusiness {
  businessId: string;
  name: string;
}

export interface TPerson {
  personId?: string;
  email: string;
  name: string;
  phone: string;
  role: string;
  join_date: string;
}
